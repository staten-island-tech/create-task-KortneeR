function RoPaSc (){

}
    //if a input is put into the terminal

    // computer pick either "rock" "paper" or "scissors"

    //if rock, check if input paper, paper = win, 
    // scissors = console.log = lose, rock = console.log = tie

    //if paper, check if input scissors, scissors = win, 
    // rock = console.log = lose, paper = console.log = tie

    //if scissors, check if input rock, rock = win, 
    // paper = console.log = lose, scissors = console.log = tie
    
    //if not rock, not paper, not scissors,
    //console.log = invalid